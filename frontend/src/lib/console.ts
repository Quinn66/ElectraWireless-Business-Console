/**
 * Layout constants for the Business Console shell.
 * Import these wherever you need to size or align elements to
 * the sidebar so that changes only require editing one place.
 */

/** Width (px) of the left nav sidebar when expanded (shows icon + label) */
export const SIDEBAR_EXPANDED_WIDTH = 200;

/** Width (px) of the left nav sidebar when collapsed (shows icon only) */
export const SIDEBAR_COLLAPSED_WIDTH = 56;

/** Width (px) of the right AI sidebar */
export const AI_SIDEBAR_WIDTH = 240;
